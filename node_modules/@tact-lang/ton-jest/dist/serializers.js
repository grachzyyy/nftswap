"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.serializers = void 0;
const core_1 = require("@ton/core");
exports.serializers = {
    test(src) {
        if (Buffer.isBuffer(src)) {
            return true;
        }
        if (src instanceof core_1.Cell) {
            return true;
        }
        if (core_1.Address.isAddress(src)) {
            return true;
        }
        return false;
    },
    print(src) {
        if (Buffer.isBuffer(src)) {
            return 'Buffer{' + src.toString('hex') + '}';
        }
        if (src instanceof core_1.Cell) {
            return src.toString();
        }
        if (core_1.Address.isAddress(src)) {
            return src.toString({ testOnly: true });
        }
        throw new Error('Unknown type');
    }
};
