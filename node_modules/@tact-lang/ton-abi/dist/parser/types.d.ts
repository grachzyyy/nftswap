import { ABIType, Slice } from "@ton/core";
export declare class TypeRegistry {
    #private;
    register(type: ABIType): this;
    get(value: number | string): ABIType | undefined;
    parse(src: Slice): any;
    private _parse;
    size(name: string): {
        bits: number;
        refs: number;
    };
}
