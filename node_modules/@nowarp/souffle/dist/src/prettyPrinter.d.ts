import { SouffleNode } from "./syntax";
/**
 * Pretty-prints Souffle entries.
 */
export declare class SoufflePrettyPrinter<FactData = undefined> {
    private addComments;
    private constructor();
    static make<FactData = undefined>({ addComments, }?: Partial<{
        addComments: boolean;
    }>): SoufflePrettyPrinter<FactData>;
    prettyPrint(node: SouffleNode<FactData>): string | never;
    private ppComment;
    private ppProgram;
    private ppFact;
    private ppRelation;
    private ppConstraint;
    private ppRule;
    private ppAtom;
}
