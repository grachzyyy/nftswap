import { BasicBlockIdx } from "../ir";
/**
 * Results of solving a generic dataflow problem.
 * @template State The type representing the state in the dataflow analysis.
 */
export declare class SolverResults<State> {
    private stateMap;
    constructor();
    getState(idx: BasicBlockIdx): State | undefined;
    setState(idx: BasicBlockIdx, state: State): void;
    getStates(): Map<BasicBlockIdx, State>;
}
