"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const cli_1 = require("./cli");
async function main() {
    const args = process.argv.slice(2);
    try {
        const result = await (0, cli_1.runMistiCommand)(args);
        if (result) {
            const [driver, mistiResult] = result;
            (0, cli_1.handleMistiResult)(driver, mistiResult);
            process.exit((0, cli_1.resultToExitCode)(mistiResult));
        }
    }
    catch (error) {
        console.error(error.message);
        process.exit(1);
    }
}
main();
